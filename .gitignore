# Ignore node_modules for both client and server
**/node_modules/

# Ignore built or compiled files for client (adjust based on your client-side framework)
client/build/
client/dist/

# Ignore server-specific files
server/node_modules/
server/build/
server/.env  # Ignore environment configuration files (if used)
